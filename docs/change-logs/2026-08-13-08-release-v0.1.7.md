# 2026-08-13-08 TokenBuddy v0.1.7 发布

## 目的

发布 `v0.1.7`：把已合入 `main` 的两批改动交付为安装包并推送给现有用户的自动更新：

- `e13da6e` Web API 防 CSRF/DNS-rebinding + 依赖漏洞升级（详见 `2026-08-13-06`）。
- `db38c31` DeepSeek Harness 数据源适配 + 面板 UI 优化（详见 `2026-08-13-07`）。

## 影响文件

- `Cargo.toml` / `apps/desktop/package.json` / `apps/desktop/src-tauri/tauri.conf.json`：版本 0.1.6 → 0.1.7。
- `Cargo.lock`：workspace 成员版本同步 0.1.7（`cargo check` 自动重写）。
- `pnpm-lock.yaml`：版本同步。

## 行为变化

- 无代码行为变化，仅版本与发布产物更新；`v0.1.7` tag 触发 Release 工作流产出 Windows MSI/NSIS 与 macOS DMG 安装包，签名更新负载写入 `latest.json`，0.1.6 用户将收到应用内更新提示。

## 验证

- `db38c31` 的 CI 五任务全绿（Frontend、Rust windows/macos、Package windows/macos）。
- 版本同步后 `cargo check` 与 `pnpm install` 通过，锁文件无旧版本残留。

## 剩余限制

- Windows 托盘、自启动、安装器体验仍归入未完成的 `Phase 4b 跨平台真机交互补验`。
