{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the TokenBuddy main window",
  "windows": ["main", "quick"],
  "permissions": [
    "core:default",
    "autostart:default",
    "core:window:allow-set-size",
    "updater:default"
  ]
}
