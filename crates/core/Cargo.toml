[package]
name = "tokenbuddy-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
chrono.workspace = true
notify.workspace = true
serde.workspace = true
thiserror.workspace = true
tokenbuddy-codex-session = { path = "../adapters/codex-session" }
tokenbuddy-claude-session = { path = "../adapters/claude-session" }
tokenbuddy-cc-switch = { path = "../adapters/cc-switch" }
tokenbuddy-cockpit = { path = "../adapters/cockpit" }
tokenbuddy-domain.workspace = true
tokenbuddy-storage.workspace = true

[dev-dependencies]
tempfile.workspace = true
